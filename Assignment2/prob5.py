"""
i have taken a string input and find its length using len() and stroed it into total_characters
and print it out.
"""
string = input("Enter a string: ")
total_characters = len(string)
print(f"Total number of characters (length): {total_characters}")